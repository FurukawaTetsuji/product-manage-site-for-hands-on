import { SignInResponseDto } from './sign-in-response-dto';

describe('SignInResponseDto', () => {
  it('should create an instance', () => {
    expect(new SignInResponseDto()).toBeTruthy();
  });
});
